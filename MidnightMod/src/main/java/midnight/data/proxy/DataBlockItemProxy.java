package midnight.data.proxy;

import midnight.common.proxy.BlockItemProxy;

// Does deliberately not extend ClientBlockItemProxy since we don't need block render types on datagen
public class DataBlockItemProxy extends BlockItemProxy {
}
